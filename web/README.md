# web — GAP 网站前端

本目录是 GAP 网站的唯一可运行代码（Next.js App Router）。**项目说明、启动方式、部署流程请见仓库根目录的 [README](../README.md)**。

常用命令（在本目录执行）：

| 命令 | 作用 |
|------|------|
| `npm run dev` | 本地开发（http://localhost:3000，热更新） |
| `npm run build` | 纯静态导出到 `out/`（生产构建，提交前需通过） |
| `npm run start` | 本地预览静态产物（需先 build） |
